package i3;

import b3.h;
import b3.m;
import java.io.Closeable;

public interface c extends Closeable {
    Iterable<m> F();

    void G(m mVar, long j10);

    i Q(m mVar, h hVar);

    int f();

    void k(Iterable<i> iterable);

    Iterable<i> m0(m mVar);

    boolean o0(m mVar);

    void r0(Iterable<i> iterable);

    long x(m mVar);
}
