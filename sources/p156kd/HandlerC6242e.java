package p156kd;

import android.os.Handler;
import android.os.Message;

public class HandlerC6242e extends Handler {
    @Override
    public void handleMessage(Message message) {
        ((C6241d) message.obj).m20976b(message.what);
    }
}
