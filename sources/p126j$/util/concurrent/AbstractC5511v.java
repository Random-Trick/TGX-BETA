package p126j$.util.concurrent;

import p126j$.util.Map;
import p126j$.util.function.BiConsumer;

public interface AbstractC5511v extends Map {
    @Override
    void forEach(BiConsumer biConsumer);
}
